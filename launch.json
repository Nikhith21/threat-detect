{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Threat Detect",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/threat_detect.py",
            "args": ["--file", "auth.log"]
        }
    ]
}